export interface SadEmotionConfig {
  recoveryRate?: number      // How quickly to recover from sadness
  isolationTendency?: number // Tendency to isolate when sad
  memoryImpact?: number      // How much sadness affects memory formation
}